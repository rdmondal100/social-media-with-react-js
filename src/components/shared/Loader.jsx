const Loader = ({className}) => {
	return <div className=' flex-center w-full '>
    <img src="/assets/icons/loader.svg" alt="loader" width={24} height={24} className={`${className}`}/>
  </div>;
};

export default Loader;
